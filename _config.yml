title: Lee Alabaster
email: leealabaster04@yahoo.com
description: My personal website for mostly blogs and ctf writeups # this means to ignore newlines until "baseurl:"
url: "https://palee.co.uk" # the base hostname & protocol for your site, e.g. http://example.com
github_username:  6c6565

collections_dir: collections

collections:
  posts: 
    permalink: /:collection/:year/:title/
    output: true
  writeups:
    permalink: /:collection/:year/:title/
    output: true


header_pages:
  - index.md
  - blog.md
  - writeups.md

#Build settings
theme: minima
plugins:
  - jekyll-feed

sass:
sass_dir: _sass

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
